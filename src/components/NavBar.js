import React from 'react'

export default function NavBar() {
    return (
        <div>
            <button onClick={() => }>Home</button>
            <button onClick={() => }>Important Tasks</button>
            <button onClick={() => }>Finished Tasks</button>
        </div>
    )
}
